# Use Apache HTTP Server on Alpine Linux
FROM httpd:2.4-alpine 

# Copy the application files to the Apache document root
COPY . /usr/local/apache2/htdocs/ 

# Expose port 80 for HTTP traffic
EXPOSE 80 